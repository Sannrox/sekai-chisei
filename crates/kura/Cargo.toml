[package]
name = "kura"
version = "0.1.0"
edition = "2021"
publish = false
description = "Canonical object store: ingest, object-sets, property ACLs, Action writeback."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
